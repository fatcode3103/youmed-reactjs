import classNames from "classnames/bind";

import styles from "./UserManage.module.scss";

const cx = classNames.bind(styles);

function UserManage() {
    return <h1>UserManage page</h1>;
}

export default UserManage;
